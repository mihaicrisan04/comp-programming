#include<bits/stdc++.h>


using namespace std;


class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        // doua cautari binare in paralel
        int l = 0, ll = 0, r = nums1.size() - 1, rr = nums2.size() - 1;
        while (l <= r && ll <= rr) {
            break;
        }
    }
};


int main() {
    Solution s;

    return 0;
}